# zsh

The interactive shell: options, history, completion, keybinds,
aliases, and tool integrations (starship, atuin, zoxide, direnv,
thefuck). Runs no framework -- see the SECTION comments inside
`.zshrc` for the handful of spots where line order actually matters.
