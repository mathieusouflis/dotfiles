# atuin

Atuin shell-history config, plus the three vendored Version 14 theme
variants in `themes/`. The config alone doesn't hook atuin into the
shell -- that needs `eval "$(atuin init zsh)"`, which lives in
`zsh/.zshrc`.
